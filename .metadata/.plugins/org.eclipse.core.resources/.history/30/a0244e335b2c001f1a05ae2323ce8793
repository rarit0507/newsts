package com.kh.spring.member.model.service;

import com.kh.spring.member.model.vo.Member;

public interface MemberService {
	//클래스의 사용법을 써두기(메서드 선언)

	int returnNum();
	// 기획 단계임! 다 정해놔야 함
	
	// 로그인(SELECT)
	private Member login(Member member);
	
	// 회원가입(INSERT)
	
	// 회원 정보 수정(UPDATE)
	
	
	// 회원 탈퇴(DELETE, UPDATE)
	
	
	// 아이디 중복체크(SELECT)
	
	
	// 메일인증
}
